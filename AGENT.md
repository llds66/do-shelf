# AGENT

本项目的前端实现遵循以下约定：

1. 组件库统一使用 `naive-ui`。
2. 样式统一使用 `UnoCSS` 工具类。
3. 主题方向统一为黑白、极简，优先通过明暗、留白、层次表达状态，避免高饱和强调色。
4. 非必要不新增手写 `scoped CSS`；确实需要覆盖第三方样式时，优先保持范围最小。
5. 新增或修改界面时，优先复用 `naive-ui` 组件与现有设计语义，保证交互和视觉风格一致。
6. 使用图标时，统一优先使用 `UnoCSS` 的 `i-...` class 图标，并使用 `div` 承载图标，不使用 `span` 或文本符号充当图标。
7. 改完代码后默认不运行 `build`；仅执行 `pnpm typecheck` 作为基础检查，除非用户明确要求额外构建或打包验证。
8. 当前项目处于开发阶段，默认不需要兼容旧数据结构代码；除非用户明确要求，否则不要添加旧结构迁移、旧 key 兜底读取或兼容分支。
